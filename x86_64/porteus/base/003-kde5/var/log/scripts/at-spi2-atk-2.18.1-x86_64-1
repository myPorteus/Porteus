if [ -e usr/share/glib-2.0/schemas ]; then
  if [ -x /usr/bin/glib-compile-schemas ]; then
    /usr/bin/glib-compile-schemas usr/share/glib-2.0/schemas >/dev/null 2>&1
  fi
fi

( cd usr/lib64 ; rm -rf libatk-bridge-2.0.so.0 )
( cd usr/lib64 ; ln -sf libatk-bridge-2.0.so.0.0.0 libatk-bridge-2.0.so.0 )
( cd usr/lib64 ; rm -rf libatk-bridge-2.0.so )
( cd usr/lib64 ; ln -sf libatk-bridge-2.0.so.0.0.0 libatk-bridge-2.0.so )
